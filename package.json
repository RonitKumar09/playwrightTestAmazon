{
  "name": "interview_assignment-e2e-automation",
  "version": "1.0.0",
  "description": "Amazon E-commerce automation framework using Playwright",
  "scripts": {
    "test": "pnpm exec playwright test",
    "test:headed": "pnpm exec playwright test --headed",
    "report": "pnpm exec playwright show-report",
    "install:browsers": "pnpm exec playwright install"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.39.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
